{
  "name": "@keyshelf/action",
  "version": "0.4.0",
  "private": true,
  "description": "GitHub Action that resolves keyshelf-managed config and secrets into $GITHUB_ENV",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/pantoninho/keyshelf",
    "directory": "packages/action"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "benchmark:runtime-deps": "node scripts/benchmark-runtime-deps.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "keyshelf": "file:../cli",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.1"
  }
}
