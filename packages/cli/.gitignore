.scratch/
oclif.manifest.json
# Generated by `npm run platforms:build` from sops-version.json: the five
# @keyshelf/sops-* packages (each carries a ~30-50MB sops binary) and the
# download cache. The committed source of truth is sops-version.json + the
# generator in scripts/, never the heavyweight binary output.
/platforms/
/.cache/
*.tgz
